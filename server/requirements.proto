syntax = "proto3";

package hello;

message HelloRequest {
    string name = 1;
}

message HelloResponse {
    string name= 1;
}

service AddService {
    rpc Compute (HelloRequest) returns (HelloResponse) {}
}
